[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    unit: pure-Python unit tests (no disk I/O, no server)
    integration: tests that need real parquet files or a running app
    e2e: end-to-end tests that run the full data pipeline
